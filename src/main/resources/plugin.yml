main: ${spigot.main}
name: ${project.name}
version: ${project.version}
description: "${project.description}"
prefix: ${spigot.prefix}
website: "${spigot.website}"
author: mfnalex
database: false
api-version: "1.13"
load: POSTWORLD

softdepend: []

permissions:
  autoshulker.craft.auto_shulker:
    description: Allow crafting auto shulker
    default: true
  autoshulker.craft.garbage_shulker:
    description: Allow crafting garbage box
    default: true

commands:
  AutoShulker: